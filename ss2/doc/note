show databases ;
create database customer;
use customer;
create  table  user (
                        id int auto_increment,
                        username varchar(100),
                        password varchar(100),
                        primary key (id)
);
select * from user;

insert into user values (1,'henok','password');
